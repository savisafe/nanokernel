# TODO

## Этап 1 — Быстрые улучшения (сразу в текущей архитектуре)
1. [x] Вынести тексты онбординга базы знаний из `DialogService` / `TelegramService` в конфиг `dialog` / `promptProfile` (убрать дубли и упростить смену языка).
2. [x] Оптимизировать использование базы знаний для экономии токенов:
   - [x] усилить retrieval-first подход;
   - [x] минимизировать вставку длинных фрагментов в prompt;
   - [x] добавить кеш/переиспользование парсинга.
3. [x] Добавить ingestion документов: `txt`, `pdf`, `docx` (MVP в Telegram-онбординге базы знаний), затем Google Docs (отдельная интеграция).
3b. [ ] Импорт базы по ссылке (Google Docs и др.): OAuth/API или экспорт; до реализации сообщения, состоящие только из URL, в режиме `/new` не сохраняются в черновик (`linkNotSupportedInDraft`).
4. [ ] Добавить напоминания (scheduled jobs через BullMQ + хранение в БД, timezone-aware).

## Этап 2 — Основа “универсального ассистента”  
5. Ввести явную модель навыков:
   - единый контракт навыка;
   - реестр доступных навыков;
   - включение/выключение навыков через конфиг сборки.
6. Сделать маршрутизацию навыков (skill routing) и разграничение зон ответственности:
   - когда отвечает базовый диалог;
   - когда вызывается специализированный навык;
   - fallback и эскалация.
7. Разработать правила (policy layer):
   - ограничения и приоритеты навыков;
   - правила безопасности;
   - правила отказа/эскалации.

## Этап 3 — Расширенные интеграции
8. Добавить интернет-поиск (deep research) с контролем источников, latency и стоимости.
9. Разработать конструктор команд (сначала DSL/JSON + валидация, затем UI при необходимости).
10. Добавить авторевью по ссылке Git (GitHub/GitLab интеграция, анализ diff, формат отчета).

TODO
1. не ищет части слитного текста
2. добавить кросс ресерч по документам (возможность добавить N документов)
3. импорт по URL (Google Docs): см. пункт 3b выше